{% extends 'base.html.twig' %}

{% block title %}New cart{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create cart</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_cart_index') }}">
                    View all carts
                </a>
            </div>
            <div class="card-body">
                 {{ include('cart/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}



