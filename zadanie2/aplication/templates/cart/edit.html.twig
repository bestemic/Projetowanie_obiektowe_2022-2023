{% extends 'base.html.twig' %}

{% block title %}Edit cart{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit cart</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_cart_index') }}">
                    View all carts
                </a>
            </div>
            <div class="card-body">
                {% include 'cart/_form.html.twig' with {'button_label': 'Update cart', 'button_color': 'btn-outline-success'}  %}
            </div>
        </div>
    </div>
{% endblock %}




